// Thin re-export hub. Actual implementation lives in core/categorize/.
// Existing imports like `import { categorizeJob } from '../core/categorize.js'`
// continue working unchanged.
export * from './categorize/index.js';
